<launch>

  <!-- We resume the logic in empty_world.launch, changing only the name of the world to be launched -->

  <include file="$(find rofous_gazebo)/launch/empty_world.launch">

    <arg name="world_name" value="$(find rofous_gazebo)/worlds/empty.world"/>

    <!-- more default parameters can be changed here -->

  </include>
  
</launch>